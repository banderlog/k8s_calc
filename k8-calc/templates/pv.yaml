apiVersion: v1
kind: PersistentVolume
metadata:
  name: exch-pv 
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/tmp/exch/"
